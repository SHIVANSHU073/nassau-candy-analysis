# Data

The raw Nassau Candy Distributor CSV is intentionally **not committed** because this repository is public.

Place the supplied CSV in this directory for local reproduction. Never commit the raw customer/order dataset.
